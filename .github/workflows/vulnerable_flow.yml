name: PR Labeler
on:
  pull_request_target:
    types: [opened]

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
      - name: Check PR Title
        run: |
          echo "Analyzing PR: ${{ github.event.pull_request.title }}"
          # Vulnerabilidad: Si el titulo contiene comillas o punto y coma, rompe el script

